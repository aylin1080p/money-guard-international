# goit-js-hw-12

## Local

1. Create `.env` with `VITE_PIXABAY_KEY=...`
2. Run `npm run dev`

## GitHub Pages

1. Add repository secret `VITE_PIXABAY_KEY`
2. Push to `main` (Actions will build and deploy `dist/`)
